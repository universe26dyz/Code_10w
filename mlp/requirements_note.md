# Environment note

Phase 1 only vendors source and MATLAB code. Python runtime requirements will
be finalized only when a Phase adds executable Python modules. Local smoke must
use `conda run -n knesvr_torch python ...` on CPU; no formal training is
authorized in this phase.
